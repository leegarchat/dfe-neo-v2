    #--俄语--#
    # 启用 `Zygisk` 模式选项：
    # 此选项将在设备启动时强制启用 `zygisk` 模式，即使首次安装了 `Magisk` 也是如此。
    # 可用选项为：
    # `false` - 禁用、`ask` - 在安装时询问、
    # `first_time_boot` - 脚本仅在首次启动时执行，首次使用信息存储在 Magisk 内存中、
    # `always_on_boot` - 脚本将在每次系统启动时执行。